# GF_SceneHost (已删除)

> **此类已在 v0.4.0 中删除。** UI 节点树创建由 `GF_UIService` 在 `configure()` 中自动完成，世界场景加载通过 `GF_SceneFactory` 直接进行，GF_WorldRoot 移至 `modules/world_root/` 作为独立的游戏世界场景基类。详见 [GF_UIService](../services/gf_ui_service.md) 和 [GF_SceneFactory](./gf_scene_factory.md)。
